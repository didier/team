<!DOCTYPE html>
<html lang="en">
  <%- include ("./partials/head") %>

  <body>
    <h1>Hello world</h1>
    <ul>
      <% users.forEach(user => { %>
      <li><%- user.name %></li>
      <% }) %>
    </ul>

    <form action="/logout" method="post"><button>Logout</button></form>
  </body>
</html>
